name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        run: pip install uv

      - name: Set up Python and install dependencies
        run: uv sync

      - name: Lint with ruff
        run: |
          uv run ruff format server/ scripts/
          uv run ruff check server/ scripts/ --fix

      - name: Type check with mypy
        run: |
          uv run pip install mypy
          uv run mypy server/ --ignore-missing-imports

      - name: Test import chain
        run: uv run python -c "from server.main import app"

      - name: Verify no missing imports in tools
        run: |
          uv run python -c "from server.db import postgres, supabase, qdrant, neo4j_client"
          uv run python -c "from server import embeddings, entity_extraction"
          uv run python -c "from server.tools import remember, search, relate, run_skill, get_evidence"
          echo "All imports OK"
